services:
  mongo:
    image: mongo:7
    container_name: yemi-bingo-mongo
    command: ["--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: echo "try { rs.status() } catch (err) { rs.initiate({_id:'rs0',members:[{_id:0,host:'127.0.0.1:27017'}]}) }" | mongosh --quiet
      interval: 5s
      timeout: 10s
      retries: 10
    restart: unless-stopped

volumes:
  mongo_data:
